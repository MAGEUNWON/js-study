let person = {
  name:'tony',
  age:20,
  driver:null
};

// if(person.age >= 16){
//   person.driver = 'yes';
// }else{
//   person.driver = 'no';
// }

let result = person.age>=16 ? "yes" : "no";
console.log(result);